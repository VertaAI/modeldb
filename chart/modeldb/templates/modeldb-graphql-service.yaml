apiVersion: v1
kind: Service
metadata:
  annotations:
  labels:
    app: modeldb
  name: modeldb-graphql
spec:
  ports:
  - port: 4000
    targetPort: 4000
  selector:
    app: modeldb
    tier: graphql
  type: ClusterIP
