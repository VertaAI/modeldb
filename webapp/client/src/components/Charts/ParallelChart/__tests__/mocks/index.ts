export const mockOnlyMetricParallelChartSelection = {
  selectedPanelElements: ['val_acc'],
};
export const mockParallelChartSelection = {
  selectedPanelElements: ['C', 'val_acc'],
};
export const mockNoDataConfig = { selectedPanelElements: [] };
export const mockMetricKeysSet = new Set(['val_acc']);
